version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: iris-redis
    ports:
      - "6379:6379"
    volumes:
      - iris-redis-data:/data
    restart: always

volumes:
  iris-redis-data:
